#include <stdio.h>
#include <stdlib.h>
int x,y;
//eto uchebnaya programma//
int main()
{
    printf("Vvedite chisla ");
    scanf ("%d,%d",&x, &y);
    printf ("%d + %d = %d\n", x,y,x+y);
     printf ("%d - %d = %d\n", x,y,x-y);
      printf ("%d * %d = %d\n", x,y,x*y);
    return 0;
}
